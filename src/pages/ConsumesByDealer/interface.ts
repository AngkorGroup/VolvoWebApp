export interface Consume {
	id: string;
	cardType: string;
	cardNumber: string;
	tpNumber: string;
	client: string;
	date: string;
	contact: string;
	currency: string;
	amount: string;
	voucherURL: string;
}
